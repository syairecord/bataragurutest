void main() {
  String input = "124345h";
  List<String> word = input.split('');
  // print(word);
  print(input);

  // for (int i = 0; i < word.length; i++) {
  //   var value = word[i];
  //   var value2;
  //   if (i + 1 == word.length) {
  //     value2 = 1;
  //   } else {
  //     value2 = word[i + 1];
  //   }
  //   if (int.parse(value) % 2 == 0 && int.parse(value2) % 2 == 0) {
  //     //word.insert(i + 1, int.parse('-'));
  //   }
  // }

  // var output = word.join();
  // print(output);
}
